import React from 'react';
import ReactDOM from 'react-dom';
 import App from './App';

 //JSX = js + xml (html dentro do js)
ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

